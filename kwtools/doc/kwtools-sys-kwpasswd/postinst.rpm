#!/bin/sh

EXEC=/usr/lib/kwtools/sys
BIN_PATH=/usr/bin

cd $BIN_PATH
for i in kwpasswd ; do
	if [ -L $i ] ; then
		rm $i &>/dev/null
		ln -sf ${EXEC}/${i} ${BIN_PATH}
	else
		ln -sf ${EXEC}/${i} ${BIN_PATH}
	fi
done

exit 0
