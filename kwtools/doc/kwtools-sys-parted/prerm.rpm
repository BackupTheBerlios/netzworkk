#!/bin/sh

DIR=/usr/share/kwtools/functions
SBIN_PATH=/usr/sbin

for i in kwparted ; do
	rm -f ${SBIN_PATH}/${i} &>/dev/null
done

for i in kwparted ; do
	rm -rf ${DIR}/${i}* &>/dev/null
done
exit 0

