#!/bin/sh

DIR=/usr/share/kwtools/functions
BIN_PATH=/usr/bin

for i in kwmutt ; do
	rm -f ${BIN_PATH}/${i} &>/dev/null
done

cd $DIR
for i in kwmutt ; do
	rm -rf ${i}* &>/dev/null
done

rm -rf ${DIR%/*/*}/doc/kwtools-net-mutt &>/dev/null
exit 0
