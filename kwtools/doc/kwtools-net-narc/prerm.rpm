#!/bin/sh

DIR=/usr/share/kwtools/functions
SBIN_PATH=/usr/sbin

rm -f ${SBIN_PATH}/kwnarcconf &>/dev/null
rm -f ${SBIN_PATH}/rckwnarc &>/dev/null
rm -rf ${DIR}/kwnarcconf* ${DIR%/*/*}/doc/kwtools-net-narc &>/dev/null
exit 0

