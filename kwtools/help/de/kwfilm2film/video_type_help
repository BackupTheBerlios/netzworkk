kwfilm2film::Konfiguration::Video_Type::Hilfe

Hier kann man zwischen den einzelnen VideoCD Typen auswaehlen. Das
ganze wird fuer mplex, in Zahlen umgerechnet.

Beispiel: svcd=4

dvd: (mit NAV Sectoren). Don't get too excited. This is really a 
        very minimal mux format. It includes empty versions of the 
        peculiar VOBU start sectors DVD VOB's include. This is enough
        to persuade some hardware players to play the stream but is a
        long way from full DVD authoring.

generic_mpeg1: Ein simples generisches MPEG1 Format, primaer fuer
        Software Decoders gedacht. Die Buffergroesse und der VBR
        muss man angeben. Die Bitrate kann automatisch erkannt
        werden.

generic_mpeg2: Das gleiche, wie generic_mpeg1, bloss MPEG2.

still_stream_vcd: VCD Stills stream (mixed mode with hi-res and stan­
        dard-res image streams multiplexed are supported).

still_stream_svcd: SVCD Stills stream.

svcd: Ueberschreibt andere Einstellungen.

user_rate_svcd: SVCD Format, aber die Buffergroesse, Bitrate und der
        VBR koennen eingestellt werden.

user_vcd: VCD Format, aber die Buffergroesse, Bitrate und der VBR
        koennen eingestellt werden.

vcd: Ueberschreibt andere Einstellungen.
